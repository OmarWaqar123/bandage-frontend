import React from 'react';
import "./Loader.scss"

import { Space, Spin } from 'antd';
const Loader = () => (
    <div className='loader'>
  
    <Spin size="large" />
    </div>

);
export default Loader;
